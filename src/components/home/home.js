import Vue from 'vue';
import {Notification} from 'element-ui';

const state = {

	show: false,
	active: '0',
	activeName: 'first',
	onresize: false,
	newsList: []
};
const actions = {
	timer: (store, n)=>{

		
	}
};

const mutations = {
	timer: (data, n)=>{

	}
}


export default {
	state,
	actions,
	mutations
}